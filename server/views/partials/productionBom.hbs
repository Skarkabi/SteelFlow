<div class="col-md-12">
    <div class="table-buttons" id="table-buttons">
        <a href="/users/create" role="button" class="btn btn-outline-dark" id="create-new"><i class="fas fa-plus"></i> Add Stock</a>
    </div>
</div>

<div class="col-md-12">
    <table id="filterable-table" class="table table-bordered table-striped">
        <thead>
            <tr>
                <th scope="col">#</th>
                {{#each attribute}}
                    <th scope="col">{{this.name}} ({{this.measurment}})</th>
                {{/each}}
                <th scope="col">Total Quantity</th>
                <th scope="col">Reserved</th>
            </tr>
        </thead>
        <tbody>
            {{#each stock}}
                <tr>
                     <td><a href="/users/view/{{this.id}}" id="userId-{{this.id}}"> {{counter @index}}</a></td>
                    {{#each this.Item_Attributes}}
                        <td><a>{{this.unit}}</a></td>
                    {{/each}}
                    <td><a href="/users/view/{{this.id}}">{{this.quantity}}</a></td>
                    <td><a href="/users/view/{{this.id}}">{{this.reserved}}</a></td>
                </tr>
            {{/each}}
        </tbody>
    </table>
</div>

<script>
    function deleteUser(id){
        if(confirm("Are you sure you want to delete this user?")){
            window.location.href = "users/delete/" + id;
        }
    }
</script>
